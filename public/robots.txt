# robots.txt for https://punds.ch
# One group covers every crawler. A crawler obeys only the single most specific
# group that matches its token, so a per-bot group would REPLACE this one for
# that bot rather than adding to it. Everything is welcome, so none is needed.

User-agent: *
Allow: /

Sitemap: https://punds.ch/sitemap.xml
